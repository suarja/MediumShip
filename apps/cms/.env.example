# MediumShip CMS web client env
# Copy to `apps/cms/.env.local` for local dev.

NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=

# Optional: if omitted, the current scripts still fall back to `../../.env.local`.
# Prefer defining the CMS values here so the sub-app is self-contained.
